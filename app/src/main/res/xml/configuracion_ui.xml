<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="retirar_preference"
        android:summary="el pedido es para retirar"
        android:title="Retirar por defecto" />
    <EditTextPreference
        android:defaultValue="mail@example.com"
        android:key="email_preference"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:summary="Se usara por defecto"
        android:title="Correo electronico" />
</PreferenceScreen>